import Dictionary from "./dictionary";

export default class Splain {
    constructor(loadDefaults) {
        this.dictionaries = new Dictionary();
        if(loadDefaults) {
            this.dictionaries.add
        }
    }
}